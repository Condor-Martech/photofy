// Placeholder — PHF-030 preenche (auto-orientação, remoção de EXIF, resize, thumbnail, versão telão).
// Lança de propósito: um enfileiramento acidental fica visível (vai para "erro"), não silenciosamente "done".
export const imageProcessor = async (job) => {
  throw new Error(`worker de imagem ainda não implementado (PHF-030): job ${job.id}`);
};
