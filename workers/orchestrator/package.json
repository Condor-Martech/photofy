{
  "name": "@photofy/orchestrator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Orquestrador BullMQ + Redis: enfileira e monitora jobs de imagem/reel (Epic 3).",
  "scripts": {
    "start": "node --env-file-if-exists=../../.env src/index.js",
    "test": "node --test"
  },
  "dependencies": {
    "bullmq": "^5.34.0",
    "ioredis": "^5.4.1"
  }
}
